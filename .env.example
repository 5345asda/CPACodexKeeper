# v1 connection secrets only. Runtime behavior belongs in config.toml.
# Process environment values override this file.

# CPA management API endpoint
CPA_ENDPOINT=https://your-cpa-endpoint.example

# CPA management bearer token
CPA_TOKEN=replace-with-management-token

# Optional HTTP/HTTPS proxy. Leave empty when direct access is required.
CPA_PROXY=
